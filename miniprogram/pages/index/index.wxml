<!--index.wxml-->
<view class="main-index">

    <view class="notice">
        <t-notice-bar visible="{{visible}}" marquee="{{marquee}}" content="{{content}}" />
    </view>

    <view class="swiper-box">
        <t-swiper autoplay="{{autoplay}}" duration="{{duration}}" interval="{{interval}}" navigation="{{navigation}}" paginationPosition="{{paginationPosition}}" height="{{400}}">
            <t-swiper-item wx:for="{{swiperList}}" wx:key="index">
                <image src="{{item.image}}" class="img" mode="aspectFill" />
            </t-swiper-item>
        </t-swiper>
    </view>

</view>